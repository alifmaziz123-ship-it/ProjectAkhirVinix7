services:
  - type: web
    name: dlas-sentiment-streamlit
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py
    autoDeploy: true
    plan: free
